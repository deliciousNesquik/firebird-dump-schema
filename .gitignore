# Local config with credentials
.env
*.env
!.env.example

# Audit logs
audit_*.log

# Generated schema dump (default DUMP_DIR)
/database/

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Локальная конфигурация uv (внутренний индекс/CA — не для публичного репо)
uv.toml
# uv.lock фиксировал внутренние URL индекса; не коммитим до перехода на публичный PyPI
uv.lock

# macOS
.DS_Store
